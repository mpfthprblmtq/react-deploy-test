import {FunctionComponent} from "react";
import {Typography} from "@material-ui/core";

const About: FunctionComponent = () => {
    return (
        <Typography>I'm an about page!</Typography>
    );
};

export default About;
